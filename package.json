{
  "name": "@keth-dev/react-lerna-demo",
  "private": true,
  "repository": {
    "type": "git",
    "url": "git+https://github.com/keth-dev/react-lerna-demo.git"
  },
  "publishConfig": {
    "registry": "https://npm.pkg.github.com"
  },
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "publish": "lerna publish patch from-git --registry https://npm.pkg.github.com"
  },
  "devDependencies": {
    "lerna": "^4.0.0",
    "prettier": "^2.3.2"
  }
}
